# Use an official Node runtime as a parent image
FROM node:18-alpine

# Set the working directory in the container
WORKDIR /app

# Copy package.json and package-lock.json (or yarn.lock) first
# This takes advantage of Docker cache layering
COPY package*.json ./

# Install project dependencies
RUN npm install

# Copy the rest of the application's source code (this will be largely overridden by volume mounts)
# However, it's good to have a base if volumes are ever misconfigured, and for node_modules to be in the right place relative to the app.
COPY . .

# Expose the port the app runs on (Vue CLI default is 8080)
EXPOSE 8080

# Command to run the Vue development server
# --host 0.0.0.0 is important to make the server accessible from outside the container
CMD ["npm", "run", "serve", "--", "--host", "0.0.0.0"] 